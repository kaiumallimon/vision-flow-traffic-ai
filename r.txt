python -m uvicorn backend.main:app --reload

python -m uvicorn backend.main:app --reload --port 8000

uvicorn backend.main:app --host 0.0.0.0 --port 8000

client id
53161265178-lm326anrb5k8hmp3ql2i5ls6kkfuti8l.apps.googleusercontent.com

npm run dev


npm run build

ngrok http 8000


ngrok http 5173


https://lilith-transposable-clarence.ngrok-free.dev



ngrok http 5173 --domain=lilith-transposable-clarence.ngrok-free.dev



http://127.0.0.1:8000/docs#/default/analyze_image_analyze_post



cd "E:\VS Code\Projects\Thesis\SW000\frontend"

cd "E:\VS Code\Projects\Thesis\SW000"  


.\venv\Scripts\Activate.ps1


# 1. Remove the already-tracked images from GitHub's memory
git rm -r --cached backend/uploads/

# 2. Add the corrected .gitignore
git add .gitignore

# 3. Commit the changes
git commit -m "Build: Fix .gitignore encoding and remove uploads from repository"

# 4. Push to GitHub
git push origin main